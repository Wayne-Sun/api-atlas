<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.api.atlas.mapper.DataSourceMapper">

    <resultMap id="BaseResultMap" type="DataSource">
        <id property="id" column="id"/>
        <result property="name" column="name"/>
        <result property="type" column="type"/>
        <result property="host" column="host"/>
        <result property="port" column="port"/>
        <result property="databaseName" column="database_name"/>
        <result property="username" column="username"/>
        <result property="password" column="password"/>
        <result property="apiKey" column="api_key"/>
        <result property="status" column="status"/>
        <result property="createdBy" column="created_by"/>
        <result property="createdAt" column="created_at"/>
        <result property="lastModifiedBy" column="last_modified_by"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <sql id="BaseColumns">
        id, name, type, host, port, database_name, username, password, api_key,
        status, created_by, created_at, last_modified_by, updated_at
    </sql>

    <sql id="ListColumns">
        id, name, type, host, port, database_name, username,
        status, created_by, created_at, last_modified_by, updated_at
    </sql>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id" parameterType="DataSource">
        INSERT INTO data_source (name, type, host, port, database_name, username, password, api_key, status,
                                 created_by, created_at, last_modified_by, updated_at)
        VALUES (#{name}, #{type}, #{host}, #{port}, #{databaseName}, #{username}, #{password}, #{apiKey}, #{status},
                #{createdBy}, #{createdAt}, #{lastModifiedBy}, #{updatedAt})
    </insert>

    <select id="selectById" parameterType="long" resultMap="BaseResultMap">
        SELECT <include refid="BaseColumns"/>
        FROM data_source
        WHERE id = #{id}
    </select>

    <select id="selectList" resultMap="BaseResultMap">
        SELECT <include refid="ListColumns"/>
        FROM data_source
        <where>
            <if test="type != null and type != ''">
                AND type = #{type}
            </if>
            <if test="status != null and status != ''">
                AND status = #{status}
            </if>
            <if test="name != null and name != ''">
                AND name LIKE CONCAT('%', #{name}, '%')
            </if>
        </where>
        ORDER BY created_at DESC
    </select>

    <select id="selectAll" resultMap="BaseResultMap">
        SELECT <include refid="BaseColumns"/>
        FROM data_source
        ORDER BY id
    </select>

    <update id="updateById" parameterType="DataSource">
        UPDATE data_source
        <set>
            <if test="name != null">name = #{name},</if>
            <if test="type != null">type = #{type},</if>
            <if test="host != null">host = #{host},</if>
            <if test="port != null">port = #{port},</if>
            <if test="databaseName != null">database_name = #{databaseName},</if>
            <if test="username != null">username = #{username},</if>
            <if test="password != null">password = #{password},</if>
            <if test="apiKey != null">api_key = #{apiKey},</if>
            <if test="status != null">status = #{status},</if>
            updated_at = #{updatedAt},
            last_modified_by = #{lastModifiedBy},
        </set>
        WHERE id = #{id}
    </update>

    <update id="updateStatus">
        UPDATE data_source
        SET status = #{status}
        WHERE id = #{id}
    </update>

    <delete id="deleteById" parameterType="long">
        DELETE FROM data_source WHERE id = #{id}
    </delete>

    <select id="countInterfacesByDataSourceId" parameterType="long" resultType="int">
        SELECT COUNT(*) FROM api_interface WHERE data_source_id = #{id}
    </select>

</mapper>
